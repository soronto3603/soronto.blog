
import Header from '../components/Header'
import Post from '../components/Posts/Post'

const Page = (props) => (
    <div>
        <Header></Header>
        <Post></Post>
    </div>
)

export default Page